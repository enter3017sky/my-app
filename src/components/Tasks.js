import React, { setState } from 'react'
import PropTypes from 'prop-types'

const Tasks = props => {
  const [aaa, setAaa] = setState([])
  return (
    <div>

    </div>
  )
}

Tasks.propTypes = {

}

export default Tasks
